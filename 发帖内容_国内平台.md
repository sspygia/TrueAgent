# 掘金 (juejin.cn) — 重写版，融入深度技术分析

直接访问：https://juejin.cn/creator/content

---

## 标题
TrueAgent：11600行代码里藏着多少精心推敲过的设计？（开源，附架构拆解）

---

## 封面摘要
不是ChatGPT套壳。七层记忆+全真递归分身+代码前置引擎+时间立体化——一个从零设计的仿生演化智能体，每一处设计都可以拆成独立教学案例。

---

## 正文

> GitHub: https://github.com/sspygia/TrueAgent  |  Gitee: https://gitee.com/sspygia/TrueAgent

我不会写代码。这个项目是我和AI通过三个月对话，一轮一轮磨出来的。代码是AI写的，架构是我设计的。

今天不聊"它多厉害"，聊五个藏在细节里的设计点——任何一个中级开发者认真读一遍，都能学到东西。

---

## 一、多Key熔断轮转：不是调用SDK，是生产级API治理

大多数Agent项目调用LLM就是一行`client.chat.completions.create()`。TrueAgent的`LLMWrapper`里是一整套API治理系统：

- **多Key轮换**：遇到429或402自动换下一个Key，避免单点失效。
- **熔断器**：连续失败达到阈值后进入`open`状态，指数退避冷却（2m→4m→8m→……），防止雪崩。
- **速率控制**：同一API调用强制间隔0.5秒，避免触发限流。
- **成功记住Key索引**：下次优先使用上次成功的Key，提高稳定性。

这些设计是从Hystrix和Resilience4j借的，但不是照搬——"成功记住Key索引"这个细节是踩了坑才加的：两个分身同时抢同一把Key导致速率超限，不加记忆会反复踩同一个坑。

---

## 二、分身启动门槛：每一层都是成本、速度、复杂度的平衡

分身系统不是"有任务就分"。TrueAgent的分身启动有精确的门槛设计：

- **重任务关键词**（`heavy_kws`）：爬取、研究、部署——单独命中就启动分身。
- **轻任务组合门槛**：需要≥3个轻量关键词（分析、整理、统计等）才触发，防止把"帮我查一下天气"也扔给分身。
- **级联模式更严格**：分身内部分身需要`heavy_kw_count ≥2`或总词数≥5才启动。为什么？因为递归分身是**指数级成本**——3层下去就是几十个并发API调用，不设门槛等于自爆。
- **用户活跃检测**：若用户30秒内有输入，后台重任务自动跳过，保证交互流畅。

而且分身不是"伪分身"——不是简单的功能转发节点。它们是**1:1完整复刻主智能体架构**，共享统一知识库/记忆/工具集，能独立思考，还能递归向下衍生新分身。五层标准配置可达3125个实例，硬件负载自动限流。

---

## 三、代码前置引擎：被低估得最厉害的部分

这是TrueAgent里最复杂、最精妙的模块。`CodeContinuationManager`不止是"让LLM写代码"，而是把**理解需求→任务拆解→分段生成→进度管理→质量评估→自愈修复**做成了一条自动化流水线。

拆开来看六个子系统：

### 1. 智能截断检测
不依赖token数量判断"写完了没"。而是检查代码的**功能完整性**：
- 是否有`def`/`class`/`import`/入口点
- 括号、缩进是否闭合
- 末尾是否以不完整语句结尾（`=`、`(`、`{`、`\`）

功能完整即使很短也认为完成；不完整即使很长也触发续写。这比LLM自己判断"我写完了吗"可靠得多。

### 2. 任务推断
根据用户描述自动拆解子任务，用**模板+关键词匹配**而非调LLM做分解：
- 爬虫：导入库→抓取→解析→存储→代理池（可选）
- API/Web：导入框架→路由→数据库→错误处理
- 数据分析：导入→清洗→统计→可视化

避免了调LLM做分解的延迟和不确定性。

### 3. 分段续写 + 去重
逐段调LLM生成补充代码，自动检测LLM是否重复了已有代码的最后几行，去掉重叠部分。这是LLM的常见毛病——会不断重复之前生成的内容——TrueAgent自动处理了。

### 4. 无进展检测 + 死锁预防
- 连续3轮无进展 → 触发元评估，决定是否继续
- 连续4轮推进同一subtask → 强制移到下一个，防止死锁

这是把**软件工程的项目管理思维**植入了Agent内部。

### 5. 质量评估与方向决策
卡死或达到最大轮次时，调LLM综合评估代码质量、进度、因果经验、执行轨迹，输出`{quality, direction, decision, reason, counterfactual}`——相当于在代码生成过程中引入了一个元评审者（meta-reviewer），避免盲目续写。

### 6. 自愈式修复
执行→捕获错误→调LLM分析错误+搜索解决方案→重写代码→重试（最多3次）。多次失败还会建议启动群策讨论，让多个分身分析根因。

**对比一下市面工具：**

| 能力 | Cursor/Copilot | AutoGPT | TrueAgent |
|------|:--:|:--:|:--:|
| 代码补全 | ✅ | ❌ | ✅ 分段续写+去重 |
| 任务拆解 | ❌ | 简单 | ✅ 模板+关键词，可靠 |
| 功能完整检测 | ❌ | ❌ | ✅ 基于语法/闭包 |
| 进度持久化 | ❌ | 部分 | ✅ 快照+续写命令 |
| 失败自愈 | ❌ | 偶尔重试 | ✅ 分析+搜索+重写 |
| 元评估决策 | ❌ | ❌ | ✅ 每轮评估继续/停止 |

现有工具要么只管"写"（Copilot），要么只管"跑"（AutoGPT），没人把"写→测→修→打包"做成闭环。TrueAgent的代码前置引擎已经接近"AI软件工程师"的雏形。

---

## 四、时间属性的立体化：让Agent拥有"时序认知"

TrueAgent把**时间作为第一维元数据**贯穿于所有子系统，形成了一条可回溯、可衰减、可预测的立体时间线：

| 层次 | 时间属性 | 作用 |
|------|----------|------|
| 每条记忆 | `timestamp` | 时效性排序和衰减 |
| 因果三元组 | `timestamp`+`count`+衰减函数 | 经验生命周期管理 |
| 知识图谱边 | `start_time`/`end_time`/`duration` | 时间窗口查询、时序路径推理 |
| 执行轨迹 | `timestamp`+`duration` | 性能分析和异常检测 |
| 反思日志 | 每条带`time` | 自我认知演变追踪 |
| 用户画像 | 改动日志带时间 | 偏好变化轨迹 |
| 日记 | 按天存储 | 连续内心独白供反思参考 |
| 状态历史 | 60秒采样 | 资源趋势+混沌值变化 |

### 其中最关键的设计：遗忘曲线公式

```python
decay = max(0.3, 1.0 / (1 + age_days / (30 + count * 10)))
```

- `count=0`（刚学到）：30天半衰期
- `count=5`（验证5次）：80天半衰期
- `count=10`（反复验证的真理）：130天半衰期，几乎不衰减

这不是纯艾宾浩斯——分母`30 + count * 10`里的`count`是使用频率修正。一个被反复验证的因果（如"语法错误→修改→通过编译"）永不衰减；过时的经验（如某API旧版本bug）30天自然淘汰。这是**经验生命周期管理**，不是静态规则库。

---

## 五、反事实推演：prompt里投资回报率最高的20个字

在计划生成的prompt里，有一行指令：

> "如果我对用户意图理解错了，他真正想要的可能是？请先想这个问题，再输出计划。"

这来自认知科学的**反事实思维**——人类做决策时自动生成的备用解释。LLM没有这个本能，必须强制注入。只有20个字，但每次都能让LLM输出质量提升一个档次。

---

## 六、有意识的工程妥协：分身消息系统不用锁

分身之间的消息传递，我故意没用文件锁，允许少量竞态条件。为什么？

- 加锁需要跨进程（文件锁或Redis），复杂度翻倍
- 死锁风险：分身A等分身B的锁，分身B等分身A的回复
- 分身消息是**辅助功能**，丢一条不影响核心任务

分布式系统里这叫"最终一致性优先于强一致性"——只不过我没用这些词，我就是觉得为了一条消息引入死锁风险不值当。这是工程权衡，不是疏忽。

---

## 普通人怎么做出来的？

我不会写代码。这个项目是我和AI三个月对话的产物。我负责设计架构、发现问题、验证逻辑；AI负责写代码、修bug、推敲细节。11600行，大概80%是架构设计和系统逻辑——不是LLM生成的模板代码。

你可能会问：不会写代码，怎么设计架构？

回答：**架构不是代码，架构是"什么应该发生"的抽象描述**。我不需要知道`def __init__(self)`怎么拼，我只需要知道"系统启动时应该检查是否有未完成的对话，如果有，恢复最近10条"。AI把这句话变成代码。我的工作是想清楚"系统应该怎样运转"，AI的工作是把这个想法翻译成Python。

这其实是一种新的创造范式：**想法→对话→代码→验证→迭代**。没有编译器，没有IDE，只有一个text input和一颗想把事情想清楚的大脑。

---

## 它不是什么

- ❌ 不是ChatGPT套壳——没有封装任何聊天API
- ❌ 不是LangChain示例项目——没有用任何Agent框架
- ❌ 不是"把API包一层的聊天UI"——有后台意识流、自主运维、自我修改

从"AI应该有记忆、有反思、能自我成长"这个命题出发，从零设计的完整系统。

---

## 诚恳求建议

早期项目，记忆和数据还在积累。特别想听这些方面的反馈：

1. **分身架构**：全真复刻vs伪分身的权衡，还有没有更好的中间方案？
2. **记忆系统**：七层记忆的贯通机制，有没有冗余或遗漏？
3. **代码前置引擎**：现有闭环还缺什么？沙箱测试和打包怎么加最优雅？
4. **时间属性**：时序知识图谱的查询语法，有没有更好的设计？

GitHub: https://github.com/sspygia/TrueAgent  
Gitee镜像（国内不卡）: https://gitee.com/sspygia/TrueAgent

欢迎star、issue、PR。
