# 2026-05-19

## 修复
- **GUI poll() 崩溃修复** (#1): `get_agent_status()` 返回的 `tools` 是 list 不是 dict，GUI 里 `tools_info.get("total")` 抛 `AttributeError` → poll 炸了 → 界面假死。改为 `len(tools_list)`。
- **GUI poll() 数据结构对齐** (#2): `memory` 用 `working`/`long` 而非 `short`/`long`；`knowledge_graph` 用 `edges` 而非 `causal`；`extensions` 字段不存在（从 `stats` 取技能数）；修复 `mem_usage` → `memory_usage`。
- **扩展加载修复** (#3): `_load_extension` 的 `exec()` 不自动提供 `__file__`，导致 ocr_skill.py 的 `os.path.dirname(os.path.abspath(__file__))` 抛 `NameError`。修复：在 namespace 传入 `"__file__": fpath`。
- **OCR 兜底修复**: ocr_skill.py 加 `try/except NameError` 兜底 `os.getcwd()`。
- **安全拦截改为软提醒** (#4): 扩展加载时的 `os.system(` 等检查从 `return`（硬拦截）改为 `print(...)`（软提醒），信任 LLM 元认知兜底。process_skill.py 成功加载。
- **全链路集成测试**: 14/14 通过（OCR/剪贴板/PC操作/进程/压缩/定时器全部正常）

## 系统状态
- 7个扩展全部正常加载
- GUI poll() 使用真实数据结构，不再崩溃
- 软提醒替代硬拦截，process_skill 带 os.system 仍正常加载
